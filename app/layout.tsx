import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const title = "Bspoke Ventures — Software Between the Software";
const description =
  "Bspoke Ventures builds focused software that automates business handoffs, reduces costs, and creates better outcomes.";

export const metadata: Metadata = {
  metadataBase: new URL("https://bspokeventures.com"),
  title,
  description,
  icons: {
    icon: "/bspoke-b.svg",
    shortcut: "/bspoke-b.svg",
  },
  openGraph: {
    title,
    description,
    type: "website",
  },
  twitter: {
    card: "summary",
    title,
    description,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
